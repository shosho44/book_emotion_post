<div class="block mr-0.5 ml-auto my-2.5 w-3/12">
  <!-- Todo: 実際にdbに保存されているかの確認。user_idをcurrent_userのidに変更-->
  <%= form_with(model: @comment, url: {controller: 'comments', action: 'create', user_id: 'test_user_1'}, local: true) do |f| %>
  <%= f.text_area :content, placeholder: 'コメント', class: 'rounded-lg bg-gray-100 h-96 mt-2.5 py-1.5 pl-1.5 break-all w-full' %>

  <%= f.submit "投稿する", class: "hover:text-gray-400 mt-2.5" %>

  <% end %>
</div>